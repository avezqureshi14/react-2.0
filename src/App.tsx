import React from 'react'
import UserProfile from './useMemo/components/API/UserProfile'

const App: React.FC = () => {
  return (
    <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center' }} >
      <UserProfile />
    </div>
  )
}

export default App
